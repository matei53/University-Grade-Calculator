[tool.black]
line-length = 100
exclude = '''
/(
    \.git
  | \.venv
  | venv
  | __pycache__
  | server/migrations
)/
'''

[tool.isort]
profile = "black"
line_length = 100
skip_glob = [
    "server/migrations/*",
    "venv/*",
    ".venv/*",
    ".git/*",
]
