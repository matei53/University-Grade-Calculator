[mypy]
# Type checking for the API only (see files = server).
# Checking the repo root fails with duplicate module names (main.py, services/, etc.).
python_version = 3.13
warn_return_any = True
warn_unused_configs = True
ignore_missing_imports = True
strict_optional = False
files = server
